{% extends 'baseUser.html.twig' %}

{% block title %}Relaxeo - Préférences{% endblock %}
  
{% block body %}
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Préférences</h1>
    </div>
   <p>Notifications par email</p>
   <form method="post" action="{{path('preferences')}}">
   <input type="hidden" name="valid" />
     <!--
     <div class="form-group row"> 
        <div class="form-check">
            <input type="checkbox" name="planningHebdo" id="planningHebdo" {% if planningHebdo == 1 %} checked="checked" {% endif %} />
            <label for="planningHebdo">Recevoir par email le planning hebdomadaire</label>  
        </div>
    </div>
    -->
   <div class="form-group row"> 
        <div class="form-check">
            <input type="checkbox" name="rappel" id="rappel" {% if rappel == 1 %} checked="checked" {% endif %} />
            <label for="rappel">Recevoir un email pour me rappeler que je suis inscrit à une séance le lendemain</label>  
        </div>
    </div>
    <div class="form-group row">               
        <div class="col-sm-4 mb-3 mb-sm-0">               
            <button type="submit" class="btn btn-success btn-user btn-block">
                Enregistrer les modifications
            </button>
        </div>          
    </div> 
    </form>    
{% endblock %}